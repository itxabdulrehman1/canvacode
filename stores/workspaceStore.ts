import { create } from 'zustand'
import type { WorkspaceDocument, WorkspaceEntry } from '@/types/workspace.types'
export const useWorkspaceStore = create<{ tree: WorkspaceEntry[]; tabs: WorkspaceDocument[]; activePath: string | null; setTree: (tree: WorkspaceEntry[]) => void; open: (document: WorkspaceDocument) => void; close: (path: string) => void }>((set) => ({ tree: [], tabs: [], activePath: null, setTree: (tree) => set({ tree }), open: (document) => set((state) => ({ tabs: state.tabs.some((tab) => tab.path === document.path) ? state.tabs : [...state.tabs, document], activePath: document.path })), close: (path) => set((state) => ({ tabs: state.tabs.filter((tab) => tab.path !== path), activePath: state.activePath === path ? state.tabs.find((tab) => tab.path !== path)?.path ?? null : state.activePath })) }))
